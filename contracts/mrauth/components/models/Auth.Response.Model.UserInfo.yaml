type: object
required:
  - email
  - lang
  - tz
  - auth_2fa_type
  - realms
  - status
properties:
  email:
    type: string
    format: email
    maxLength: 64
    example: "user-email@example.com"
  phone:
    type: string
    format: phone
    maxLength: 32
    example: "+7 999 888 77 66"
  lang:
    type: string
    maxLength: 5
    example: "ru-RU"
  tz:
    description: >-
      Часовой пояс пользователя (IANA-имя), сохранённый в его профиле.
      Даты/время в ответах форматируются по поясу, зафиксированному в access-токене,
      поэтому после смены настроек и до перевыпуска токена значения могут отличаться
    type: string
    maxLength: 64
    example: "Europe/Moscow"
  auth_2fa_type:
    $ref: "../enums/Auth.Enum.UserAuth2fa.yaml"
  realms:
    type: array
    items:
      $ref: "./Auth.Response.Model.UserRealm.yaml"
  status:
    $ref: "../enums/Auth.Enum.UserStatus.yaml"
